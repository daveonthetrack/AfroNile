# Local Audio Assets Directory

Copy your album `.mp3` files directly into this directory.

For example, if you place a file named `nile_waves.mp3` here, it will be served locally at:
`http://localhost:3000/audio/nile_waves.mp3`

You can then update the `audioUrl` in the database (or the seed script) to `/audio/nile_waves.mp3`.
